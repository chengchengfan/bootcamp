import pytest
import numpy as np
import eq_calculator

def test_eq_conc():

    # cab = 0.5 * (ca0 + cb0 + Kd - np.sqrt((ca0 + cb0 + Kd) **2 - 4 * ca0 * cb0))
    # ca = ca0 - cab
    # cb = cb0 - cab
    #
    # assert Kd == ca * cb / cab
    # assert ca0 == ca + cab
    # assert cb0 == cb + cab

    



    pytest.raises(RuntimeError, "eq_calculator failed")

    return none
